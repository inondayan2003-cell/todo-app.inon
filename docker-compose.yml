version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: todo-db
    restart: always
    environment:

      MYSQL_DATABASE: tododb

      MYSQL_ROOT_PASSWORD: password
    ports:
      - "3306:3306"